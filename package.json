{
  "name": "sarah-eule-worker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Unbeaufsichtigte SARAH-Worker für server-2: (1) Eulen-Video-Untertitel (ffmpeg) und (2) X/TikTok-Engagement (Playwright).",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  },
  "scripts": {
    "start": "node worker.mjs",
    "einmal": "node worker.mjs --einmal",
    "engagement": "node engagement-worker.mjs beide",
    "engagement:probe": "ENGAGEMENT_DRYRUN=1 node engagement-worker.mjs beide",
    "login": "node engagement-login.mjs beide"
  }
}
